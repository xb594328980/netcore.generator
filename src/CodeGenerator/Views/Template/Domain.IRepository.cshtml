@model TableConfig
    <pre>
using System;
using System.Collections.Generic;
using System.Text;
using Sansunt.HiCard.Domain.Models;

namespace Sansunt.HiCard.Domain.Interfaces
{
    /// <summary>
    /// 接口
    /// des:@Model.FullName
    /// create by @ViewData["Auth"].ToString() @DateTime.Now.ToString("yyyy/MM/dd")
    /// </summary>
    public interface I@(Model.FullName)Repository : IRepository<@(Model.FullName)>
    {
    }
}
</pre>